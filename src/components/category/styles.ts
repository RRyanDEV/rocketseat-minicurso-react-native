import { StyleSheet } from "react-native";

export const stylesCategory = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
        gap: 5,
    },
    name: {
        fontSize: 16,
        fontWeight: "bold",
    }
})